{
  "name": "sarconx-site",
  "version": "1.0.0",
  "private": true,
  "description": "Sito SarconX — AI Automation Agency Pavia & Milano",
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o styles.css --minify",
    "watch:css": "tailwindcss -i src/input.css -o styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "puppeteer-core": "^25.3.0"
  }
}
